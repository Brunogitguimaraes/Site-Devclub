Coloque aqui o banner principal do site com o nome `hero-banner.png`.

Como adicionar a imagem:
1. Salve a imagem que você anexou (Hero) como `hero-banner.png`.
2. Copie para `apps/web/src/assets/hero-banner.png`.

Alternativamente, se preferir que eu adicione o arquivo, envie a imagem em formato PNG ou JPG e eu a salvarei aqui automaticamente.
